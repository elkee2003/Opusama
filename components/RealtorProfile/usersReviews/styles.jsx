import { StyleSheet, } from 'react-native'

const styles = StyleSheet.create({
    noReviews:{
        marginTop:60,
        fontSize:30,
        fontWeight:'bold',
        color:'#b6b3b3',
        textAlign:'center',
    },
})

export default styles;