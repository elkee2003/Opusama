import { StyleSheet,} from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex:1,
    },
    marker:{
        fontSize:30,
        color:'#04043a',
    },
})

export default styles;
